{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "copy to remote, compile, copy files back locally, and start node with debugger",
      "type": "shell",
      "command": "ssh pi@poolcontroller-001.local 'cd relayEquipmentManager; tsc'; rsync  -avzh  pi@poolcontroller-001.local:/home/pi/relayEquipmentManager/dist /Users/rgoldin/programming/relayEquipmentManager --include='./dist/*'; ssh pi@poolcontroller-001.local 'cd relayEquipmentManager; sudo killall node; sudo node --inspect-brk=0.0.0.0 dist/app.js'",
      "problemMatcher": [],
      "dependsOn": [
        "copy to remote"
      ]
    },
    {
      "label": "copy to remote",
      "type": "shell",
      "command": "rsync -v -z -h -p --no-o --no-g  -r * pi@poolcontroller-001.local:/home/pi/relayEquipmentManager --exclude dist --exclude 'data' --exclude node_modules --exclude .cache",
      "problemMatcher": []
    },
    {
      "label": "copy from remote",
      "type": "shell",
      "command": "rsync  -avzh  pi@poolcontroller-001.local:/home/pi/relayEquipmentManager/dist /Users/rgoldin/programming/relayEquipmentManager  --include='./dist/*'",
      "problemMatcher": [],
      "dependsOn": [
        "copy to remote"
      ]
    },
  ]
}